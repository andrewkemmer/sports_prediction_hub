streamlit>=1.42.0
plotly>=5.20.0
numpy>=1.22.0
# Heavy C-extension packages (xgboost, lightgbm, scikit-learn) are
# intentionally omitted: they require compilation tools and frequently
# timeout or OOM on Streamlit Cloud free-tier builds.  The engine has
# pure-Python fallback implementations for all three (engine/tree_ensemble.py).
